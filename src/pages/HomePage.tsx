import React from 'react';
import PropTypes from 'prop-types';

const HomePage = props => {
    let nolla = 0;
    nolla += 0

    return (
        <div>

        </div>
    );
};

HomePage.propTypes = {

};

export default HomePage;